# No project-specific lint thresholds yet; clippy defaults apply. Add entries
# here if the matcher grows functions that trip clippy's argument/complexity
# thresholds. The crate stays pure-Rust with no runtime dependencies.
